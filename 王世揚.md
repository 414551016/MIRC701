# 王世揚
論文主題：Engage-Agent：基於 MITRE Engage 的雙層 LLMAgent 交戰式蜜網系統

[📄論文主題：Engage-Agent：基於 MITRE Engage 的雙層 LLMAgent 交戰式蜜網系統](Papers/王世揚/Owen_thesis_0503.pdf)
{開啟新視窗：[Ctrl] + Click}
```
Q：請幫我用簡單方式說明這篇論文的：
1.主要研究問題
2.方法做了什麼
3.有什麼創新
4.跟以往方法差在哪
-----------------------------------------------
Q：幫我分析這篇論文：
核心 idea 是什麼
方法流程可以一步一步解釋嗎
為什麼這樣設計（設計動機）
有沒有關鍵公式或模型重點
實驗是怎麼驗證的
-----------------------------------------------
Q：幫我分析這篇論文：優點與缺點
優點與缺點
有哪些假設不一定成立
有沒有可能改進的地方
如果換成其他場景還適用嗎
和其他方法相比的 trade-off
-----------------------------------------------
Q：他們的what及how，更重要的是why，也就是他們在解決什麼問題，為何要解這個問題，如何解決、跟別人差別，為何要這樣解。請從你的看法講對此論文多少why來判斷你的了解夠不夠透徹。
```
# Q：請幫我用簡單方式說明這篇論文的：
## 1. 主要研究問題
```
這篇論文想解決的問題是：
現有 honeypot / honeynet 雖然可以觀察攻擊者，但不太會「主動引導」攻擊者往更深層的攻擊行為前進。

更具體地說：
傳統 honeypot 通常只模擬單一主機，所以很難觀察像是橫向移動、憑證重用、Active Directory 攻擊這種多階段、多主機攻擊。Honeynet 雖然有多台主機，比較像企業環境，但多數系統仍偏向被動觀察，缺乏根據攻擊者行為即時調整環境、誘導攻擊者繼續探索的能力。

所以本文的核心問題是：
如何設計一個能在真實多主機 AD honeynet 中，根據攻擊者行為動態修改環境，並主動引導攻擊進展的自適應 deception 系統？
```
## 2. 方法做了什麼
```
這篇論文提出一個系統叫 Engage-Agent。

它的核心做法是：
用兩層 LLM Agent 來判斷攻擊者意圖，然後透過 MCP 工具修改真實 honeynet 環境。

流程可以簡化成這樣：
攻擊者輸入指令後，SSH proxy 會攔截並記錄近期指令。接著系統把這些指令交給第一層 LLM，也就是 Strategist LLM。Strategist 會判斷攻擊者目前想做什麼，例如探索主機、找憑證、嘗試提權或橫向移動，然後選擇合適的防禦誘導策略與工具。
之後第二層 Planner LLM 會把 Strategist 的高階策略轉成具體工具參數，例如要在哪台主機放置假憑證、建立假帳號、放 breadcrumb、修改服務或製造可控錯誤。最後 MCP toolkit 會根據這些參數產生 Ansible playbook，實際修改 AD honeynet 中的主機環境。

重點是：
系統不是直接假裝回覆攻擊者，而是真的改變環境，讓攻擊者後續看到的結果來自真實系統狀態。
```
## 3. 有什麼創新
```
這篇論文的創新主要有四點。
第一，把 adaptive deception 從單機 honeypot 擴展到多主機 honeynet。過去很多 adaptive honeypot 只針對單一 SSH shell 或單一主機互動，本文則處理 AD domain、Windows、Linux、SQL、mail、web server 等多主機環境。
第二，使用雙層 LLM 架構。Strategist 負責理解攻擊者意圖與選工具，Planner 負責產生穩定的工具參數。這樣比單一 LLM 同時做所有事情更容易控制，也比較適合本地小模型部署。
第三，採用 environment-level deception，而不是 reply-level deception。也就是說，系統不是單純產生假的文字回覆，而是實際改變檔案、帳號、憑證、服務與跨主機線索，讓 deception 更一致、更能承受攻擊者後續驗證。
第四，把 MITRE Engage 用來指導 deception 行為。系統不是隨機放假資料，而是根據 MITRE Engage 的活動設計工具，例如 decoy identity、breadcrumb、credential surface、controlled failure 等，讓誘導策略更有結構。
```
## 4. 跟以往方法差在哪
```
可以分成三類比較。

跟傳統 honeypot 比：
傳統 honeypot 多半是單機、靜態、被動等待攻擊者互動。本文的方法是多主機、動態、主動引導攻擊者，而且目標是讓攻擊者走到更深的攻擊階段。

跟一般 honeynet 比：
一般 honeynet 雖然有多台主機，比較像企業網路，但通常主要是建環境和收 log，缺少根據攻擊者行為即時調整環境的能力。本文則加入 LLM Agent，可以根據攻擊者目前行為修改 AD honeynet。

跟 LLM / RL adaptive honeypot 比：
像 QRASSH、HoneyGPT 這類系統可以根據攻擊者指令產生互動，但多半是單機或回覆層級操作。本文強調的是跨主機協調，而且不是只改回覆文字，而是實際改環境，因此比較適合觀察橫向移動、憑證濫用、AD 攻擊等企業級攻擊流程。

一句話總結：
這篇論文的重點不是做一個會聊天的 honeypot，而是做一個會根據攻擊者行為，動態改變整個 AD honeynet 的主動交戰系統。
```


# Q：幫我分析這篇論文：
```
以下是比較完整、但仍用簡單方式整理的分析。先提醒：這份 PDF 目前第 6 章 Experiment Result 的實際內容看起來還沒有完整寫入；目錄有列出第 6 章與表 6、表 7，但可讀到的正文主要停在第 5.4 節，因此實驗部分我會區分「論文已明確寫出的驗證設計」與「目前缺少的正式結果」。
```
## 1. 核心 idea 是什麼？
```
這篇論文的核心 idea 是：
不要只做一個會回話的 honeypot，而是做一個會根據攻擊者行為，動態改變整個 AD honeynet 環境的交戰式防禦系統。
傳統 honeypot 通常是單機、被動觀察；一般 honeynet 雖然是多主機，但多半還是偏靜態環境。本文想解決的缺口是：如何同時具備「企業級多主機真實性」和「根據攻擊者行為主動互動」的能力。論文指出，既有方法缺少把語意推理、跨主機協調決策、環境層級執行整合在一起的框架，因此提出 Engage-Agent。

最關鍵的一句話是：
攻擊者看到的結果不是 LLM 編出來的文字，而是因為系統真的改了 honeynet 環境，所以後續指令自然看到新的檔案、帳號、憑證、服務或線索。
這稱為 environment-level deception。論文強調它比單純 reply-level deception 更穩，因為攻擊者後續可以驗證檔案、帳號、權限、服務與跨主機關係；如果只是文字假回覆，很容易被拆穿。
```
## 2. 方法流程一步一步解釋
整體流程可以分成 8 步。
- Step 1：攻擊者連進 honeynet
```
攻擊者透過 SSH entry point 連進系統。中間有一個 SSH proxy，負責攔截與記錄攻擊者輸入的指令。系統不是每一個指令都孤立判斷，而是累積近期 command context。
```
- Step 2：SSH proxy 收集近期攻擊行為
```
例如攻擊者可能先執行：
whoami
hostname
ls
cat /etc/passwd
find / -name "*password*"

# 單看某個指令不一定知道攻擊者意圖，但連起來看，就可能判斷他正在做 discovery 或 credential access。
```
- Step 3：Strategist LLM 判斷攻擊者意圖
```
第一層 LLM 叫 Strategist LLM。它的輸入是近期 command context Ci 和可用 MCP tools。它會做三件事：
1.推論攻擊者目前想做什麼。
2.決定下一步 engagement strategy。
3.選擇適合的工具集合 Ti。

# 這一層的輸出不是直接改環境，而是高階策略，例如「攻擊者正在找高權限帳號，所以建立一個假的高權限帳號線索」。
```
- Step 4：Planner LLM 把策略轉成具體計畫
```
第二層 LLM 叫 Planner LLM。它接收 Strategist 的策略 ai、選好的工具 Ti，以及 MCP tool specification Ts，然後產生結構化計畫 P。
簡單說，Strategist 決定「要做什麼」，Planner 決定「怎麼做」。
例如：
Strategist 說：
  「放置一個 credential breadcrumb，引導攻擊者往 SQL server 移動。」
Planner 會轉成具體參數：
  「在 Ubuntu client 的 /home/user/notes.txt 放入 fake SQL credential，指向 sql01.corp.local。」
```
- Step 5：MCP toolkit 驗證工具參數
```
Planner 不直接產生 Ansible 腳本，而是只產生工具參數。MCP module 會檢查參數是否符合 schema，例如工具名稱是否存在、必要欄位是否完整、型別是否正確。這是為了避免 LLM 亂產生不可執行的腳本。
```
- Step 6：MCP toolkit 產生 Ansible playbook
```
MCP toolkit 會把 Planner 的結構化計畫 P 套進 Jinja2-based Ansible template，產生真正可以執行的 playbook。接著透過 Ansible 對 AD honeynet 裡的主機進行修改。
```
- Step 7：AD honeynet 被實際修改
```
環境可能被修改的內容包括：
帳號、群組、權限、假憑證、decoy file、breadcrumb、服務設定、controlled failure 等。論文的 AD honeynet 包含 domain controller、Windows hosts、Linux hosts、web server、SQL server、mail server 等，因此可以支援比較真實的跨主機攻擊流程。
```
- Step 8：攻擊者後續看到被修改後的真實環境
```
攻擊者下一次執行指令時，看到的不是 LLM 假裝出來的結果，而是 target host 真實回傳的結果。這使 deception 更一致，也比較能支撐多步驟攻擊，例如 credential access、privilege escalation、lateral movement。
```
## 3. 為什麼這樣設計？設計動機是什麼？
- 動機一：單機 honeypot 不夠支援企業級攻擊

  現代攻擊常常包含 lateral movement、credential reuse、AD compromise 等多階段行為。單機 honeypot 很難觀察這種跨主機攻擊鏈。論文一開始就指出，傳統 honeypot 因為通常只模擬單一主機，所以攻擊深度和行為觀察都受限制。

- 動機二：一般 honeynet 比較真實，但太被動

  Honeynet 可以提供多主機環境，但既有系統多半重點在部署、收集 log、觀察攻擊，而不是根據攻擊者意圖主動改變環境。論文認為這造成「環境真實性」和「互動智慧」之間的缺口。

-  動機三：只改回覆文字不可靠

  如果系統只是用 LLM 產生假的 shell output，短期看起來很像真的，但攻擊者後續可以驗證。例如系統回覆「有一個 admin 帳號」，但攻擊者真的查 /etc/passwd 或 AD users 時找不到，就會露餡。本文因此選擇實際修改環境，而不是直接偽造回覆。

- 動機四：不能讓 LLM 直接寫腳本

  LLM 直接產生 Ansible 腳本很容易出錯，例如語法錯、參數錯、主機設定不一致。論文因此採用 tool-based execution：每個 MCP tool 都包裝一個已驗證的 Ansible template，LLM 只需要產生參數。這樣能提高執行穩定性。

- 動機五：雙層 LLM 比單一 LLM 容易控制

  單一 LLM 如果同時負責理解攻擊者、決定策略、選工具、產生部署參數，任務太大，也不利於本地模型低延遲部署。論文把任務拆成 Strategist 和 Planner：Strategist 專注語意理解與策略，Planner 專注穩定產生工具參數。

## 4. 有沒有關鍵公式或模型重點？
  
  有。這篇不是數學模型很重的論文，但有一個重要的問題形式化。

- 4.1 問題被定義成 sequential decision problem

  在每個互動步驟 i，系統觀察攻擊者近期指令：
  ```
  Ci = recent attacker command context
  ```
  然後決定如何修改 honeynet，讓攻擊者看到的回應 Ri 能引導他往更深的攻擊階段前進。論文把 adaptive deception 定義成一個 persistent multi-host environment 上的 sequential decision problem。

- 4.2 核心變數

  重要符號可以這樣理解：
  |符號	|意義  |
  |-----|------|
  |Ci	|第 i 步觀察到的近期攻擊指令脈絡  |
  |H = {H1, ..., HNH}	|honeynet 中多台主機集合  |
  |ai	|Strategist 產生的高階 engagement strategy  |
  |Ti	|Strategist 選出的工具集合  |
  |P = (p1, ..., pNH)	|Planner 產生的跨主機計畫  |
  |S	|MCP 產生的可執行腳本  |
  |E	|執行結果  |
  |Ri	|攻擊者最後觀察到的回應  |
  整體可以簡化成：
  ```
  近期攻擊指令 Ci
  -> Strategist 推論意圖與策略 ai、工具 Ti
  -> Planner 產生計畫 P
  -> MCP 產生腳本 S
  -> AD honeynet 被修改
  -> 攻擊者看到回應 Ri
  ```

- 4.3 目標函數：讓攻擊走得更深

  論文的目標是最大化攻擊者到達的攻擊階段：
  ```
  maximize max si
  其中 si ∈ {1, 2, ..., 7}，代表第 i 步攻擊者到達的攻擊階段。這 7 個階段在模擬器中是：
  discovery
  collection
  credential access
  privilege escalation
  lateral movement
  exfiltration
  impact
  
  論文的 attacker simulation 也是根據這七階段來引導攻擊者模型產生下一步命令。
  ```

- 4.4 約束條件：可靠性與即時性
  ```
  論文設定兩個重要限制。
  第一是執行可靠性：
    execution success rate > 0.9
    也就是多主機環境修改不能常常失敗。
  第二是即時性：
    tstrategist + tplanner + texecution <= 90 seconds
    這表示 Strategist 推理時間、Planner 推理時間、環境修改執行時間加起來要在 90 秒內。
  ```

- 4.5 模型重點：Strategist 用 prompt，Planner 用 SFT

  Strategist 需要彈性語意理解，所以論文認為比較適合 prompt engineering，不對 Strategist 做 fine-tuning。Planner 則需要穩定產生符合 schema 的 tool parameters，因此使用 supervised fine-tuning。

  Planner 的 fine-tuning 使用 Phi-4 作為 base model，QLoRA-style SFT，4-bit loading，只訓練 LoRA adapter，資料切分為 240 train / 30 validation / 30 test。  

## 5. 實驗是怎麼驗證的？
  這部分要分成「已經寫清楚的驗證設計」和「目前文件缺少的結果」。

- 5.1 已寫清楚的驗證設計
  論文的驗證設計主要有三塊。
  - 第一，建立一個 AD-domain honeynet 環境。這個環境包含 domain controller、Windows hosts、Linux hosts、web、SQL、mail server 等，目的是模擬企業網路，讓攻擊者可以進行 credential access、privilege escalation、lateral movement 等多階段攻擊。
  - 第二，建立 Planner 的訓練資料集。論文從 5 個人工設計的 AD-domain attack scenarios 開始，每個 scenario 有 24 個攻擊指令，再擴增成 100 個 scenario variants，切成 300 個 stage-level samples。接著用 4 個 teacher LLM 產生 Strategist 與 Planner candidates，最後保留 300 筆 validated Planner SFT examples。
  - 第三，用 LLM-driven attacker simulator 驗證系統。這個 attacker simulator 不是固定 replay script，而是根據前面看到的 session history 和回應動態產生下一個攻擊指令。它依照七階段攻擊流程前進，並會根據環境中發現的證據調整行為。

- 5.2 預期應該驗證哪些指標？
  根據論文的 problem formulation，合理的實驗指標應該至少包含：
  |指標	|目的  |
  |-----|-----|
  |attack stage progression	|看攻擊者是否被引導到更後面的攻擊階段  |
  |execution success rate	|看 MCP / Ansible 修改環境是否穩定  |
  |latency	|看 Strategist + Planner + execution 是否在 90 秒內  |
  |schema compliance	|看 Planner 是否能產生合法工具參數  |
  |behavioral diversity	|看攻擊者行為是否更多樣  |
  |cross-host consistency	|看跨主機線索是否前後一致  |

  其中 attack stage progression、execution reliability、real-time response constraint 是論文形式化問題中明確提到的重點。

- 5.3 目前文件中的問題：正式實驗結果不足

  目前這份 PDF 的目錄顯示第 6 章是 Experiment Result，表格清單也列出 Table 6、Table 7、Table 8、Table 9，但表名仍像是 placeholder，例如「This is a table」和「Comparison on model efficiency and model size」。

  而目前可讀到的正文主要到第 5.4 節 attacker interaction simulation，沒有看到完整的第 6 章數據、baseline 比較、消融實驗或統計結果。因此，這篇目前比較像是方法與系統設計已經完整，但實驗結果章節尚未補齊。

## 總結一句話
  
  這篇論文的核心貢獻是：用 Strategist LLM + Planner LLM + MCP toolkit，在 AD-based honeynet 中根據攻擊者行為動態修改真實環境，讓攻擊者被自然引導到更深層、多階段、跨主機的攻擊流程。

  但目前版本的弱點是：實驗結果章節看起來還不完整，缺少明確數據來證明 attack depth、成功率、延遲與 baseline 比較。


# Q：幫我分析這篇論文：優點與缺點
  下面是針對這篇論文的批判式分析，重點放在優缺點、隱含假設、可改進方向、跨場景適用性，以及和其他方法的 trade-off。
## 1. 優點
- 優點一：研究問題選得合理

  這篇論文抓到一個重要缺口：**傳統 honeypot 通常是單機，難以觀察 lateral movement、credential reuse、AD compromise 這類多階段企業攻擊**；honeynet 雖然有多主機環境，但多數仍偏被動觀察，缺少主動引導攻擊者的能力。論文把問題定位成「企業級真實性」與「自適應互動能力」之間的落差，這個問題意識是合理的。

- 優點二：environment-level deception 比 reply-level deception 更可信

  本文最強的設計是：**不直接偽造攻擊者看到的 shell output，而是實際修改 honeynet 環境**。這樣攻擊者後續如果檢查檔案、帳號、服務、權限或跨主機關係，看到的結果比較能保持一致。論文也明確指出，reply-level manipulation 在 persistent multi-host setting 中容易被後續驗證拆穿，因此改採 environment modification。

  這點是很大的優勢，因為在真實攻擊流程中，攻擊者不是只看一次輸出，而是會反覆驗證。例如他看到一組 credential 後，可能會拿去登入其他主機；如果只是文字上假裝有 credential，**但環境裡沒有對應帳號，就會失真**。

- 優點三：雙層 LLM 架構切分清楚

  Strategist LLM 負責理解攻擊者意圖、選策略與工具；Planner LLM 負責產生可執行的工具參數。這種 separation of concerns 很合理。論文也說明，如果用單一模型同時處理意圖理解、策略決策、工具選擇與部署細節，任務會太寬、難控制，也不利於本地低延遲部署。

  這個設計的好處是：Strategist 可以保持語意彈性，Planner 則可以被 fine-tune 成穩定輸出 schema-compliant tool parameters。

- 優點四：MCP/tool-based execution 提高可執行性

  論文沒有讓 LLM 直接產生 Ansible script，而是讓 LLM 產生工具參數，再由已驗證的 template 渲染成 playbook。這降低了 LLM 產生錯誤語法、錯誤參數、不可執行腳本的風險。論文也明確指出，free-form script generation 會導致 invalid syntax、inconsistent configuration 和低成功率，因此採 tool-based approach。
  
  這對資安系統很重要，因為 deception action 如果執行失敗，不只是實驗失敗，也可能破壞環境一致性。
  
- 優點五：選 AD honeynet 作為場景有代表性

  Active Directory 是企業攻擊裡非常核心的 attack surface。論文選 AD 作為 honeynet substrate，可以支援 credential abuse、privilege escalation、lateral movement 等高價值攻擊流程。這比單純 Linux SSH honeypot 更接近企業攻擊。

## 2. 缺點
- 缺點一：實驗結果目前不完整

  目前 PDF 的目錄有第 6 章 Experiment Result，也列出 Table 6、Table 7、Table 8、Table 9，但表格名稱看起來仍是 placeholder，例如「This is a table」與「Comparison on model efficiency and model size」。這代表目前版本的方法設計比實驗驗證完整，正式數據還不足。

  這是最大問題。因為這篇論文的主張是「可以增加 attack depth、behavioral diversity、realism」，但如果沒有完整 baseline comparison、ablation study、統計結果，很難說服讀者。
  
- 缺點二：太依賴 LLM 對攻擊意圖的判斷

  Strategist 的核心任務是從近期 command context 推論 attacker intention。但攻擊者行為可能很 noisy、故意混淆、使用自動化工具、或執行看似無關的探索指令。LLM 如果誤判意圖，就會選錯 deception strategy，甚至暴露出不自然的誘餌。

  例如攻擊者只是列目錄，Strategist 卻過早放出高權限 credential breadcrumb，可能會顯得太刻意。
  
- 缺點三：工具空間 bounded，穩定但彈性有限

  MCP tool 是 predefined capability。這提高可靠性，但也限制 deception action 的多樣性。當攻擊者走出預設攻擊路徑，或嘗試未涵蓋的服務與協定時，Planner 只能在既有工具集合內選擇，可能無法產生合適反應。

  簡單說：bounded tool space 換來穩定性，但犧牲了 open-ended adaptability。
  
- 缺點四：系統工程成本高

  這個方法需要 AD domain、多主機 honeynet、SSH proxy、LLM inference service、MCP toolkit、Ansible template、tool validation pipeline、attacker simulator。相比一般 honeypot，它的部署、維護、除錯成本高很多。

  尤其 AD 環境本身就複雜，若要維持多主機狀態一致性、快照回復、帳號權限、domain trust、服務設定，工程負擔會很大。
  
- 缺點五：安全風險需要更明確處理

  系統會動態建立假帳號、假憑證、服務線索或跨主機 breadcrumb。若隔離不完整，這些 deception artifacts 可能被攻擊者用來進一步移動，甚至造成 honeynet 逃逸風險。論文有強調 controlled environment，但還需要更完整的 safety model，例如權限邊界、網路隔離、egress control、credential non-reusability。

## 3. 哪些假設不一定成立？
- 假設一：攻擊者會被 breadcrumb 引導

  論文假設攻擊者看到假帳號、假憑證、假檔案、服務線索後，會順著系統希望的方向走。但真實攻擊者不一定如此。有些攻擊者會快速判斷這是 honeypot，有些會用自動化工具掃描，有些可能只做破壞或丟 malware，不一定會探索設計好的誘導路徑。

- 假設二：近期 command context 足以判斷意圖

  Strategist 使用 recent command context 推論 attacker intention。這假設近期指令能代表攻擊者真正目標。但攻擊者可能先做大量 generic enumeration，或同時測多條路徑。單靠最近幾個 command 可能不足以分辨「正在收集資訊」還是「準備橫向移動」。

- 假設三：環境修改能在可接受時間內完成

  論文問題設定中有即時性限制：tstrategist + tplanner + texecution ≤ 90 seconds。但在真實多主機 AD 環境中，Ansible/WinRM/SSH 執行時間可能受主機負載、網路、Windows policy、domain replication 影響。90 秒不一定穩定成立。

- 假設四：工具執行成功率能維持高水準

  論文設定 execution reliability 要大於 0.9。這在 controlled lab 可能可行，但在更複雜的企業拓撲、異質服務、Windows/Linux 混合環境中，工具失敗率可能上升。

- 假設五：LLM attacker simulator 可以代表真實攻擊者

  實驗設計使用 LLM-driven attacker simulator，而不是完全依賴真人 red team。這有助於自動化測試，但 LLM attacker 的行為可能偏向 prompt 中定義的七階段 attack progression，未必代表真實攻擊者的策略、工具鏈、耐心程度與 honeypot detection behavior。


## 4. 有沒有可能改進的地方？
- 改進一：補完整實驗與 baseline

  最需要補的是實驗。建議至少比較以下 baseline：
  |Baseline	|比較目的  |
  |---------|---------|
  |static honeynet	|證明動態 deception 是否真的增加 attack depth  |
  |reply-level LLM honeypot	|證明 environment-level deception 是否更一致  |
  |rule-based environment modification	|證明 LLM semantic reasoning 是否有幫助  |
  |single LLM agent	|證明雙層 Strategist/Planner 是否比單模型好  |
  |no MITRE Engage mapping	|證明 Engage-guided design 是否有貢獻  |
  
  指標可以包含 attack stage reached、session length、unique TTPs、lateral movement attempts、credential reuse attempts、tool execution success rate、latency、attacker suspicion rate。
  
- 改進二：加入 ablation study

  這篇系統有多個 component，應該拆開驗證：

  移除 Strategist，只用固定策略；移除 Planner SFT，只用 prompting；移除 environment modification，改用 reply fabrication；移除 MITRE Engage mapping，改用 arbitrary tools。這樣才能證明每個設計真的有貢獻。
  
- 改進三：加入 state rollback / snapshot 機制

  動態修改 honeynet 後，環境可能越來越髒。建議加入 snapshot、transaction、rollback、版本化狀態管理。每次 deception action 都應該能記錄：改了哪台主機、哪個檔案、哪個帳號、哪個服務，以及如何復原。

- 改進四：加入 risk-aware planning

  Planner 不應該只追求攻擊深度，也要考慮安全風險。例如建立假帳號的權限不能太高、credential 不能可用於真實資源、service exposure 不能造成 breakout。可以加入 risk score：
  ```
  score = engagement_gain - risk_cost - execution_cost
  ```
  這會讓系統更接近可部署的 defensive deception platform。

- 改進五：加入真人 red team 評估

  LLM attacker simulator 很適合大量測試，但最終仍需要真人攻擊者或 CTF/red-team style evaluation。真人會更常做 honeypot fingerprinting、嘗試奇怪工具、使用非預期攻擊路徑，能測出系統是否真的 robust。

- 改進六：讓 MITRE ATT&CK 與 Engage 雙向結合

  目前論文主要用 MITRE Engage 指導 defender action。可以進一步把 attacker command 對應到 ATT&CK TTP，再由 TTP 映射到 Engage activity。這樣 Strategist 的決策會更可解釋：
  ```
  command context -> ATT&CK technique -> attacker stage -> Engage activity -> MCP tool
  ```

## 5. 如果換成其他場景還適用嗎？
  適用，但需要重做 environment model 和 toolset
  ```
  這個方法的抽象架構是可遷移的：
  attacker behavior observation
  -> intent inference
  -> engagement strategy
  -> bounded tool planning
  -> real environment modification
  -> attacker observes changed environment
  ```
  這個架構不只適用 AD honeynet，也可以改到其他場景。但 MCP tools、環境模型、攻擊階段、誘餌設計都要重做。

- 場景一：ICS / OT honeynet

  可以適用，但難度更高。ICS 場景需要模擬 PLC、HMI、Modbus、OPC UA、physical process。environment modification 不只是放檔案或帳號，而可能要改 sensor value、process state、control logic。這會牽涉 safety constraint，所以 risk-aware planning 更重要。
  
- 場景二：Cloud honeynet

  很適合延伸到 AWS/Azure/GCP。工具可以改成建立假 IAM role、假 S3 bucket、假 access key、假 metadata service、假 Kubernetes secret。優點是 cloud API 很適合 tool-based execution；缺點是成本與防止真實濫用更重要。
- 場景三：Web application deception

  也適用。Strategist 可以判斷攻擊者在做 SQL injection、path traversal、credential stuffing 或 admin panel discovery；Planner 則改變假資料庫、假 session、假 API response、假後台功能。不過 web 場景的互動頻率更高，90 秒 execution latency 可能太慢。
- 場景四：Malware sandbox

  可部分適用。系統可以根據 malware behavior 動態生成假檔案、假 registry key、假 domain controller、假 C2 artifacts。但 malware 往往是自動化行為，不像真人攻擊者會被語意 breadcrumb 引導，所以效果可能較弱。
- 場景五：IoT honeynet

  可以適用，但要針對 device protocol 和 firmware behavior 設計工具。例如假 camera stream、假 MQTT topic、假 device credential、假 telnet service。不過 IoT 攻擊常常是 botnet 自動掃描，互動式 LLM 策略的價值可能不如 AD 或 cloud 場景。

## 6. 和其他方法相比的 trade-off





















